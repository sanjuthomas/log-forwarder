Log Forwarder — Integration Test Cases
========================================

This document lists integration test scenarios for log-forwarder. Cases marked
AUTOMATED are implemented under internal/integration/ and run in CI via
go test ./... and go test -race ./...

Run integration tests:

  go test ./internal/integration/ -v
  go test ./...                    # includes integration package


AUTOMATED — End-to-end (watcher + pipeline + sink)
--------------------------------------------------

ID    Test name                              Scenario
--    ---------                              --------
E2E-1 TestE2E_SpringBootSingleLineToFileSink
                                             Spring Boot INFO line: multiline parser,
                                             regex transform, static + host enrichers,
                                             file sink JSONL output.

E2E-2 TestE2E_SpringBootMultilineStackTrace
                                             ERROR + 3-line stack trace becomes one
                                             JSON record with multiline message field.

E2E-3 TestE2E_WatermarkResumeAfterRestart
                                             Publish 3 lines, stop forwarder, append 2
                                             lines, restart with same watermark file;
                                             sink has 5 records (no re-publish of first 3).
                                             Uses line parser so each line commits
                                             immediately (multiline holds the last event
                                             until flush).

E2E-4 TestE2E_LogRotation
                                             Tail app.log, rotate (rename + new file),
                                             both before-rotate and after-rotate lines
                                             appear in sink. Does not cover copytruncate
                                             (same inode, in-place truncate) — unsupported;
                                             see README § Watermarks / log rotation.

E2E-5 TestE2E_TransformOnErrorSkip
                                             Malformed line skipped; valid tab-delimited
                                             line published once.

E2E-6 TestE2E_TransformOnErrorWrap
                                             Malformed line published as {_raw, _path,
                                             _error} wrapper record.

E2E-7 TestE2E_HTTPSinkReceivesJSON
                                             Watcher + pipeline posts JSON to httptest
                                             HTTP ingest server.

E2E-8 TestE2E_PublishRetryRecovers
                                             Injected sink failures (2) then success;
                                             record eventually written via pipeline retry.

E2E-9 TestE2E_MetricsHealthAndCounters
                                             Metrics server /health returns 200;
                                             /metrics exposes lines_read and
                                             lines_published after one line processed.

E2E-10 TestE2E_FilterErrorsOnlyToFileSink
                                             Tab-delimited INFO/WARN/ERROR lines;
                                             ERROR-only filter publishes one record.

E2E-11 TestE2E_FilterMatchAnyPassesWarnAndError
                                             match: any filter passes WARN and ERROR,
                                             drops INFO.

E2E-12 TestE2E_FilterIncrementsMetricsCounter
                                             Filtered lines increment
                                             log_forwarder_lines_filtered on /metrics.

E2E-13 TestE2E_FilterWatermarkAdvancesOnFilteredLines
                                             INFO-only input with ERROR filter:
                                             no sink records, watermark still advances.

E2E-14 TestE2E_FilterOnMissingDropExcludesWrappedRecords
                                             transform.on_error: wrap record without
                                             level field is filtered out (on_missing drop).

E2E-15 TestE2E_WatermarkStallsWhenPublishFails
                                             Always-failing sink with max publish attempts:
                                             watermark file has no entry for the log file.

E2E-16 TestE2E_WatermarkResumesWithoutDuplicateAfterPublishFailure
                                             Line one published; line two fails to publish;
                                             watermark unchanged during failure; recovery
                                             publishes line two only (no duplicate line one).

E2E-17 TestE2E_DoubleBufferBurstWithSlowSink
                                             Publish batch enabled with small max_bytes;
                                             slow file sink; 12-line burst completes with
                                             double-buffer overlap (async flush while ingest
                                             continues).

E2E-18 TestE2E_HibernateOnPublishBatchFailure
                                             Always-failing sink with publish_batch +
                                             on_flush_failure: hibernate; /ready NOT_READY
                                             (sink_hibernating); /health still 200.

E2E-19 TestE2E_DeadLetterOnPublishFailure
                                             dead_letter mode with failing sink: JSONL batch
                                             on disk, watermark advanced, consecutive DLQ
                                             counter incremented, forwarder not hibernating.

E2E-20 TestE2E_DebouncedWatermarkReplayAfterCrash
                                             watch.state.flush_interval 100ms with delayed
                                             disk persist; crash stop without flush; restart
                                             re-publishes lines within the debounce window.

E2E-21 TestE2E_DebouncedWatermarkGracefulStopNoReplay
                                             debounced watermark with graceful stop (final
                                             flush); restart appends new lines only with no
                                             duplicate publish of earlier records.


AUTOMATED — Publish batch unit tests (internal/pipeline)
--------------------------------------------------------

ID    Test name                              Scenario
--    ---------                              --------
PB-1  TestPublishFlusherAsyncOverlapUnderBurst
                                             Double buffer: ingest continues while async
                                             flush is in flight.

PB-2  TestPublishFlusherBlocksWhenActiveFullDuringFlush
                                             Backpressure when active buffer is full and
                                             flush is still running.

PB-3  TestPipelineHibernatesAfterBatchFlushFailure
                                             Batch flush failure enters hibernate; watermark
                                             stalled.

PB-4  TestHibernateWakeRetriesAndRecovers (hibernate_wake_test.go)
                                             wake_enabled retries stalled batch and exits
                                             hibernate on success.

PB-5  TestDeadLetterAdvancesWatermarkOnFlushFailure
                                             dead_letter mode writes JSONL and advances
                                             watermark when sink publish fails.

PB-6  TestDeadLetterHibernatesAtConsecutiveLimit
                                             Consecutive dead-letter batches without sink
                                             success transition to hibernate.

PB-7  TestDeadLetterWatermarkStalledOnWriteFailure
                                             Dead letter disk write fails: watermark not
                                             advanced, no hibernate.

PB-8  TestPipelineSyncPublishSkipsBatchFailurePolicies
                                             publish_batch disabled (sync publish): flush
                                             failure policies (hibernate/dead_letter) do not
                                             apply; Run returns error instead.


AUTOMATED — Configuration
-------------------------

ID    Test name                              Scenario
--    ---------                              --------
CFG-1 TestIntegration_ExampleConfigsValidate
                                             All configs/*.yaml and examples/kafka/*.yaml
                                             load and pass Validate() without error.

KAFKA-1 scripts/kafka-smoke.sh (CI on every PR to main; also workflow_dispatch)
                                             docker-compose.kafka.yaml round-trip with
                                             ERROR-only filter: one record on topic logs,
                                             WARN filtered, lines_filtered metric present.

KAFKA-2 scripts/kafka-deadletter-smoke.sh (CI with KAFKA-1)
                                             docker-compose.kafka-dlq.yaml: real Kafka sink,
                                             broker stopped after startup, failed publish
                                             writes JSONL to /dlq, dead_letter metrics,
                                             GET /deadletters metadata (no record bodies),
                                             line not on topic after broker restart.

DLQ-1  TestE2E_DeadLettersEndpointMetadata
                                             In-process forwarder with metrics: GET /deadletters
                                             returns batch metadata after dead letter write;
                                             response does not include record bodies.

SMOKE-1 scripts/docker-smoke.sh
                                             docker-compose.smoke.yaml file-sink stack with
                                             ERROR-only filter: one JSONL record, filtered
                                             metric present.


MANUAL / STAGING ONLY — Not automated (and why)
-----------------------------------------------

ID    Scenario                               Why manual
--    ---------                              ----------
M-1   Real Kafka publish (TLS/SASL/SCRAM)    PLAINTEXT round-trip automated via
                                             scripts/kafka-smoke.sh; TLS/SASL still manual

M-2   Kafka topic lag under sustained load   Needs broker metrics and multi-hour soak.

M-3   Network partition to remote sink       Hard to simulate reliably in unit CI;
                                             use toxiproxy or staging chaos.

M-4   kill -9 mid-publish boundary           OS/process timing dependent; verify in
                                             staging with duplicate-detection at consumer.

M-5   Disk full on watermark or sink path    Requires filesystem quota manipulation.

M-6   Two forwarders, same watermark file    Operational anti-pattern; document only.

M-7   GSSAPI / Kerberos Kafka                Config validates; sink auth not implemented.

M-8   OAuth2 HTTP ingest                     http-noauth only; custom sink required.

M-9   24–72 hour soak at production volume   CI time/cost; run in staging pipeline.

M-10  Prometheus alert rule firing           Requires Prometheus + Alertmanager stack.

M-11  systemd Restart=on-failure recovery    Host-level supervisor; smoke on deploy target.

M-12  Cross-platform rotation (Windows)      inode semantics differ; test on target OS.


RECOMMENDED STAGING ADDITIONS (future automation)
-------------------------------------------------

These could be automated later with docker-compose or testcontainers:

  - Kafka sink round-trip: forwarder -> Kafka -> consumer asserts JSON  [DONE: kafka-smoke.sh]
  - Publish failure sustained: stop broker, assert watermark stall + metric alerts  [PARTIAL: E2E-15, E2E-16]
  - Debounced watermark crash/restart replay window  [DONE: E2E-20, E2E-21]
  - Load generator: scripted append loop + assert buffer_depth stays bounded
  - File sink concurrent rotation under poll interval stress


MULTILINE PARSER AND WATERMARKS (operator notes)
------------------------------------------------

Documented in README § Multiline parser and watermarks. Summary:

  - Multiline records commit on the next start_pattern line or graceful shutdown.
  - Watermarks advance per committed parser event, not per physical line read.
  - The stored offset is the end of the last line in the committed record.
  - Buffered continuation lines are read from disk but do not publish or advance
    the watermark until commit.
  - Trailing stack traces at EOF need a following header line or shutdown flush.

Test workarounds:

  - E2E-2 appends a flush-sentinel header line so the ERROR stack trace commits.
  - E2E-3 uses parser.type: line so each line commits immediately for restart tests.
  - waitForRecordCountFlush (harness) stops the forwarder on timeout to flush the
    multiline parser buffer during tests.


TEST MATRIX (sink x parser)
---------------------------

                    line parser    multiline parser
file sink           E2E-5,6,10-21  E2E-1,2,3,4
http-noauth sink    E2E-7          (same as E2E-1 pattern — add if needed)
kafka sink          KAFKA-1, manual M-1 (TLS/SASL)   KAFKA-1 (PLAINTEXT + filter)
docker file smoke   SMOKE-1        —


OPERATOR NOTES (documented, not automated)
------------------------------------------

  - copytruncate log rotation (logrotate in-place truncate, same inode) is unsupported.
    Use create/rename rotation. Documented in README § Watermarks / log rotation.

  - pipeline.on_full: drop causes permanent log loss when the watcher→pipeline buffer is
    full. Dropped lines do not reach the pipeline or watermarks and are not replayed on
    restart. Default to block in production. Documented in README § pipeline.on_full.

  - Alert on rate(log_forwarder_pipeline_buffer_dropped[5m]) > 0 when drop mode is enabled.


PLANNED — Config combination tests
----------------------------------

See docs/config-test-plan.txt for the full matrix, priorities (P0–P3), and GitHub
issue mapping. Highlights:

  E2E-27  on_full block backpressure + readiness buffer_threshold
  E2E-28  on_full drop + buffer_dropped + watermark skip
  E2E-29  publish_timeout
  E2E-30  hibernate wake_enabled recovery
  E2E-31  readiness reasons on live forwarder
  CFG-2   table-driven runtime test for all example YAML configs


COVERAGE GAPS (acceptable for v1)
---------------------------------

  - No test for pipeline buffer full / on_full: block backpressure  → E2E-27
  - No test for on_full: drop offset/watermark interaction          → E2E-28
  - No test for copytruncate rotation (unsupported; documented in README)
  - No test for multiple watch sources in one process               → E2E-22
  - No test for custom registered parser/transform/sink
  - Metrics test does not assert exact counter values (presence only)
  - Example YAML configs validate only (CFG-1); no runtime table     → CFG-2
